Generic single-database configuration with an async dbapi.

```
alembic current
alembic revision --autogenerate -m "Create a baseline migrations"
```

```
docker run -e MYSQL_HOST=192.168.1.110 tracardi/migration:0.9.0-dev
```